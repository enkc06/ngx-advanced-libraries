import { UniqByCollectionPipe } from './uniq-by-collection.pipe';

describe('UniqByCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqByCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});

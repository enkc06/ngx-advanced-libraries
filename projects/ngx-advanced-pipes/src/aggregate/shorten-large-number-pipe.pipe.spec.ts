import { ShortenLargeNumberPipe } from './shorten-large-number-pipe.pipe';

describe('ShortenLargeNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenLargeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});

import { DistanceBetweenOriginsPipe } from './distance-between-origins.pipe';

describe('DistanceBetweenOriginsPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceBetweenOriginsPipe();
    expect(pipe).toBeTruthy();
  });
});
